{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "description": "Used to express the dynamic constraints on transport parameters. These constraints may be set and changed at run time. Parameters must also conform with constraints inferred from the specification. Every transport parameter must have an entry, even if it is only an empty object.",
  "title": "Constraints",
  "anyOf": [
    {
      "type": "array",
      "items": {
        "$ref": "constraints-schema-sender-rtp.json"
      }
    },
    {
      "type": "array",
      "items": {
        "$ref": "constraints-schema-sender-rtp_tcp.json"
      }
    },
    {
      "type": "array",
      "items": {
        "$ref": "constraints-schema-sender-udp.json"
      }
    },
    {
      "type": "array",
      "items": {
        "$ref": "constraints-schema-sender-srt.json"
      }
    },
    {
      "type": "array",
      "items": {
        "$ref": "constraints-schema-sender-usb.json"
      }
    },
    {
      "type": "array",
      "items": {
        "$ref": "constraints-schema-sender-ndi.json"
      }
    },
    {
      "type": "array",
      "items": {
        "$ref": "constraints-schema-websocket.json"
      }
    },
    {
      "type": "array",
      "items": {
        "$ref": "constraints-schema-mqtt.json"
      }
    }
  ]
}
